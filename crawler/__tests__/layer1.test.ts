import { describe, test } from "@jest/globals";

describe("tests for the layer 1 of our crawler", () => {
  test.todo("a simple search should yield a relevant result");
  test.todo("a vague search should yield a different but still simple result")
  test.todo("the layer1 result should be unfiltered and dirty")
})
